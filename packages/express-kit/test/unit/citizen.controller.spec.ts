import { mockCities, mockCitizens } from '../mock-data/data';

describe('Test CitizenController', () => {
  it('Should be return all citizens', async () => {
    expect(2).not.toBeInstanceOf(String);
  });

  xit('Should be return "Hello Lerna" message', () => {});
});
